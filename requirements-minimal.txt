# Minimal requirements for AI Research Lab Simulator
# Only essential packages - no optional dependencies

# Core LangChain (already installed, but listing for reference)
# langchain==0.3.0
# langchain-openai==0.2.0
# langchain-community==0.3.0
# langgraph==0.2.20
# langsmith==0.1.137

# Essential dependencies
pydantic-settings>=2.1.0
PyYAML>=5.3
langchain-core==0.3.79
langchain-text-splitters==0.3.11
langchain-anthropic/claude-3-haiku==0.2.0

# Supporting packages (minimal set)
requests>=2.0.0
tenacity>=8.1.0,!=8.4.0

# Note: These are optional and can be installed later if needed
# streamlit>=1.28.0  # For UI
# chromadb>=0.4.18  # For vector store
# fastapi>=0.104.1  # For API
# uvicorn>=0.24.0  # For API server

